namespace Harion.CustomKeyBinds.Components {
    public enum KeyPage {
        TurnLeft,
        TurnRight,
        Default,
        Nothing
    }

    public enum MovePage {
        MoveDown,
        MoveUp,
        Default,
        Nothing
    }
}
